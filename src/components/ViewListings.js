import React from 'react';
import Listing from './Listing';
// import { useDispatch, useSelector } from 'react-redux';

const ViewListings = () => {
  // const dispatch = useDispatch();
  // const post = useSelector(state => state.listingReducer.postListing);
  // const remove = useSelector(state => state.listingReducer.deleteListing);
  // const get = useSelector(state => state.listingReducer.getListing);
  
  return (
    <div>
      <h1>ViewListings</h1>
    </div>
  );
};

export default ViewListings;